Subtype: How-To / Step-by-Step
Goal: Teach one practical skill to the target market, tied to the startup product when relevant.

REQUIRED JSON SCHEMA:

  "hook": "string (short attention-grabbing opener)",
  "problem": "string (contextual problem statement for the target market)",
  "steps": ["array of step strings (3-7 steps)"],
  "example": "string (a short, industry-relevant example that references the startup or its use-case)",
  "cta": "string (call-to-action, e.g. 'Try X', 'Learn more at...', or 'Save this checklist')"


WRITING NOTES:
- Keep items punchy, actionable.
- Each step should be a single sentence.
- Example must mention the industry or target market explicitly.
